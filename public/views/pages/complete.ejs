<!doctype html>
<html lang="pt-br" translate="yes">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap"
      rel="stylesheet"
    />

    <link rel="stylesheet" type="text/css" href="/styles/global.css" />

    <link
      rel="stylesheet"
      type="text/css"
      href="/styles/components/spinner.css"
    />

    <link rel="stylesheet" type="text/css" href="/styles/forms.css" />
    <link rel="stylesheet" type="text/css" href="/styles/pages/complete.css" />

    <script defer type="module" src="/scripts/complete.js"></script>

    <title>Informações da conta</title>
  </head>
  <body data-id="app">
    <div class="container" data-id="container">
      <div class="app" data-id="app">
        <div class="form-wrapper">
          <form class="form" data-id="form">
            <fieldset class="form-field-groups">
              <legend class="form__title">
                Informações da conta

                <span class="form__subtitle"
                  >Estamos quase lá! Finalize a sua inscrição preenchendo os
                  compos abaixo com seus dados pessoais.</span
                >
              </legend>

              <div class="form-group">
                <label for="name" class="form-group__label">
                  Nome completo
                </label>
                <input
                  id="name"
                  name="name"
                  type="text"
                  required
                  autocomplete="off"
                  autofocus
                  placeholder="Digite seu nome completo"
                  class="form-group__input"
                  minlength="8"
                />
              </div>

              <div class="form-group">
                <label for="username" class="form-group__label">
                  Apelido e/ou nome de usuário (username)
                </label>
                <input
                  id="username"
                  name="username"
                  type="text"
                  required
                  autocomplete="off"
                  placeholder="Degite seu username e ou/apelido"
                  class="form-group__input"
                  minlength="5"
                />
              </div>
            </fieldset>

            <button class="form-button" type="submit">
              <span class="form-button__text">Finalizar cadastro</span>
              <div class="spinner" data-id="spinner"></div>
            </button>
          </form>
        </div>
        <aside class="illustration" data-id="ilustration"></aside>
      </div>
    </div>

    <div class="spinner-loading" data-id="loading">
      <div class="spinner"></div>
    </div>
  </body>
</html>
